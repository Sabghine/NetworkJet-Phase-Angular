import { RegistrationRequest } from './registration-request';

describe('RegistrationRequest', () => {
  it('should create an instance', () => {
    expect(new RegistrationRequest()).toBeTruthy();
  });
});
